export default function calcVendorPrice() {
  // console.log(`VALUE ${product.priceThreshold[1].amount}`);
  // const thresholds = product.reduce((previousValue, currentValue) => {
  //   if (previousValue.indexOf(currentValue) === -1) {
  //     previousValue.push(currentValue);
  //   }
  //   return previousValue;
  // }, []);
}
